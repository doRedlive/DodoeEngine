{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Cakery",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "--preset",
        "msvc-editor-debug"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$msCompile"
      ]
    },
    {
      "label": "Build DodoeSandbox",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "--preset",
        "msvc-sandbox-debug"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build"
      },
      "problemMatcher": [
        "$msCompile"
      ]
    }
  ]
}
